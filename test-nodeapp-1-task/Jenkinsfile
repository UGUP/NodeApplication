pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                sshagent(['sshcredentials5']) {
                    git branch: 'main',
                    credentialsId: 'sshcredentials5',
                    url: 'git@github.com:UGUP/NodeApplication.git'
                }
            }
        }
    }
}
